Backup script to save on tape drives

Requirements:
  - Python 3 (or higher)
  - YAML library (see below)
  - LXML library (see below)

Install yaml and LXML pachages with:
  - pip install yaml
  - pip install lxml
